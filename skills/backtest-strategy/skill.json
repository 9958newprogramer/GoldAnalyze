{
  "name": "backtest-strategy",
  "version": "1.0.0",
  "description": "将自然语言均线策略编译为受约束规格，锁定数据版本后调用独立确定性 Backtest Engine。",
  "allowed_tools": [
    "resolve_backtest_data_version",
    "execute_backtest",
    "summarize_result"
  ],
  "max_tool_calls": 3,
  "steps": [
    "interpret_strategy",
    "resolve_backtest_data_version",
    "cache_lookup",
    "execute_backtest",
    "summarize_result"
  ]
}
